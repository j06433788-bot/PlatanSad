# PlatanSad - Інтернет-магазин розсадника рослин 🌿

## Опис проекту
PlatanSad - це повнофункціональний інтернет-магазин для продажу декоративних та хвойних рослин. Додаток включає каталог товарів, кошик для покупок, оформлення замовлень та список бажань.

## Технології

### Backend
- **FastAPI** - сучасний веб-фреймворк для Python
- **PostgreSQL** - реляційна база даних (мігровано з MongoDB)
- **SQLAlchemy** - ORM для роботи з БД
- **Asyncpg** - асинхронний драйвер PostgreSQL
- **Pydantic** - валідація даних

### Frontend
- **React 19** - бібліотека для побудови UI
- **React Router** - маршрутизація
- **Tailwind CSS** - utility-first CSS фреймворк
- **Radix UI** - доступні компоненти UI
- **Axios** - HTTP клієнт
- **Sonner** - toast notifications
- **Context API** - управління станом

## Основні функції

### ✅ Реалізовано:

1. **Каталог товарів**
   - Перегляд усіх товарів
   - Фільтрація за категорією, ціною, типом
   - Пошук за назвою та описом
   - Сортування (за назвою, ціною)

2. **Деталі товару**
   - Повна інформація про товар
   - Зображення та опис
   - Вибір кількості
   - Інформація про наявність

3. **Кошик**
   - Додавання товарів до кошика
   - Зміна кількості
   - Видалення товарів
   - Автоматичний підрахунок загальної суми

4. **Список бажань (Wishlist)**
   - Додавання/видалення товарів
   - Швидке додавання до кошика
   - Зберігання між сесіями

5. **Оформлення замовлення**
   - Форма з контактними даними
   - Вибір способу доставки (Нова Пошта / Самовивіз)
   - Вибір способу оплати (Накладений платіж / Картка)
   - Валідація форми

6. **Адаптивний дизайн**
   - Mobile-first підхід
   - Responsive верстка для всіх екранів
   - Оптимізована навігація для мобільних

7. **UI/UX покращення**
   - Smooth анімації та transitions
   - Toast нотифікації
   - Loading states
   - Hover ефекти
   - Sticky header

## API Endpoints

### Products
- `GET /api/products` - отримати всі товари (з фільтрами)
- `GET /api/products/{id}` - отримати товар за ID
- `POST /api/products` - створити товар (Admin)
- `PUT /api/products/{id}` - оновити товар (Admin)
- `DELETE /api/products/{id}` - видалити товар (Admin)

### Categories
- `GET /api/categories` - отримати всі категорії
- `GET /api/categories/{id}` - отримати категорію за ID

### Cart
- `POST /api/cart/add` - додати товар до кошика
- `GET /api/cart` - отримати кошик
- `PUT /api/cart/{item_id}` - оновити кількість
- `DELETE /api/cart/{item_id}` - видалити товар
- `DELETE /api/cart/clear/{userId}` - очистити кошик

### Wishlist
- `POST /api/wishlist/add` - додати до списку бажань
- `GET /api/wishlist` - отримати список бажань
- `DELETE /api/wishlist/{item_id}` - видалити зі списку

### Orders
- `POST /api/orders` - створити замовлення
- `GET /api/orders` - отримати замовлення користувача
- `GET /api/orders/{id}` - отримати замовлення за ID

### Health
- `GET /api/health` - перевірка стану API

## Структура проекту

```
/app/
├── backend/
│   ├── server.py          # FastAPI додаток
│   ├── database.py        # SQLAlchemy моделі та конфігурація
│   ├── models.py          # Pydantic моделі
│   ├── seed_data.py       # Скрипт для завантаження даних
│   ├── migrate_mongo_to_postgres.py  # Скрипт міграції
│   ├── requirements.txt   # Python залежності
│   └── .env              # Змінні оточення
│
└── frontend/
    ├── src/
    │   ├── api/           # API клієнти
    │   ├── components/    # React компоненти
    │   ├── context/       # Context providers
    │   ├── pages/         # Сторінки
    │   ├── App.js         # Головний компонент
    │   └── index.js       # Entry point
    ├── public/            # Статичні файли
    ├── package.json       # Node залежності
    └── .env              # Змінні оточення (REACT_APP_BACKEND_URL)
```

## Запуск проекту

### Backend
```bash
cd /app/backend
python seed_data.py  # Завантажити дані
sudo supervisorctl restart backend
```

### Frontend
```bash
cd /app/frontend
yarn install
sudo supervisorctl restart frontend
```

### Перевірка статусу
```bash
sudo supervisorctl status
```

## Environment Variables

### Backend (.env)
```
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/platansad
MONGO_URL=mongodb://localhost:27017  # Backup only
DB_NAME=test_database  # Backup only
CORS_ORIGINS=*
```

### Frontend (.env)
```
REACT_APP_BACKEND_URL=https://archive-unpacker-4.preview.emergentagent.com
```

## База даних PostgreSQL

### Таблиці:
- **products** - товари
- **categories** - категорії
- **cart** - кошик
- **wishlist** - список бажань
- **orders** - замовлення
- **quick_orders** - швидкі замовлення

### Міграція з MongoDB:
Для міграції даних з MongoDB в PostgreSQL використовуйте:
```bash
cd /app/backend
python migrate_mongo_to_postgres.py
```

## Особливості реалізації

1. **Асинхронна робота з PostgreSQL** - використання SQLAlchemy async + asyncpg для ефективної роботи з БД
2. **UUID замість ObjectID** - для простішої серіалізації та роботи з Frontend
3. **Context API** - для глобального управління станом (кошик, wishlist)
4. **React Router v7** - для навігації між сторінками
5. **Responsive Design** - mobile-first підхід з Tailwind CSS
6. **Toast Notifications** - sonner для user feedback
7. **Data Persistence** - дані зберігаються в PostgreSQL між сесіями
8. **JSON fields** - для зберігання масивів badges та orders items

## Майбутні покращення

- [ ] Авторизація користувачів
- [ ] Адмін панель
- [ ] Історія замовлень
- [ ] Порівняння товарів
- [ ] Відгуки та рейтинги
- [ ] Інтеграція з платіжними системами
- [ ] Email нотифікації
- [ ] Pagination для каталогу
- [ ] Full-text search в PostgreSQL

## Контакти

**PlatanSad**
- Телефони: +380 (63) 650-74-49, +380 (95) 251-03-47
- Email: info@platansad.ua
- Адреса: Дубенський рн. Смт. Смига, Рівне, Україна

---

**Розроблено з ❤️ для PlatanSad**
**Версія 2.0 - PostgreSQL Edition**
